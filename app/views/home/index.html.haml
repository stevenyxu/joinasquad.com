#home
  .container
    .row
      .span5
        %h1 Find friends. Play together.
        :markdown
          Welcome to _joinasquad.com_, a service built to help gamers find other gamers.
      .span11
        %ul.tabs(data-tabs="tabs")
          - if user_signed_in?
            %li.active
              %a(href="#getting-started") Getting started
          - else
            %li.active
              %a(href="#join") Join
            %li
              %a(href="#login") Login
          %li
            %a(href="#about") About
        .tab-content
          - if user_signed_in?
            #getting-started.tag-pane.active(name="getting-started")
              %h2 Getting started
          - else
            #join.tab-pane.active(name="join")
              = render :partial => 'devise/registrations/form'
            #login.tab-pane(name="login")
              = render :partial => 'devise/sessions/form'
          #about.tab-pane(name="about")
            %h2 About us
