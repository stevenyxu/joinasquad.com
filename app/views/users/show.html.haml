#user.container{'data-id' => @user.id, 'data-type' => User}
  - if can? :write, @user
    = link_to 'Edit', [:edit, @user]
  %h1= @user.email
  %section
    %h2 Games
    - @user.games.each do |game|
      = game.name
